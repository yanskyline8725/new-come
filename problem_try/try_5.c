#include<stdio.h>


 int main() {
    int n;
    printf("请输入班级的方阵（大小n*n）：");
    scanf("%d",&n);
    printf("请输入方阵中每个人的性别（中间用空格隔开，一行一行输入，女生为0，男生为1）；");
    int x[n];
    int y[n];
    for (int h=0;h<n,h++); 
    { 

    }
 }